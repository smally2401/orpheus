export component TrackPlayingInfo {
    in property <string> current-track-title: "No song playing";
    in property <string> current-artist: "---";
    in property <image> current-art;

    in property <color> now-playing-song-text-color;
    in property <color> now-playing-artist-text-color;
    in property <int> now-playing-song-text-size;
    in property <int> now-playing-artist-text-size;

    HorizontalLayout {
        spacing: 10px;

        VerticalLayout {
            alignment: center;
            padding-left: 10px;
            Image {
                source: root.current-art;
                width: 45px;
                height: 45px;
            }
        }

        VerticalLayout {
            min-width: 0;
            alignment: center;
            padding-top: 10px;
            padding-left: 10px;

            Text {
                text: root.current-track-title;
                color: root.now-playing-song-text-color;
                font-size: root.now-playing-song-text-size * 1px;
                horizontal-alignment: left;
                overflow: elide;
            }

            Text {
                text: root.current-artist;
                color: root.now-playing-artist-text-color;
                font-size: root.now-playing-artist-text-size * 1px;
                horizontal-alignment: left;
                overflow: elide;
            }
        }
    }
}
